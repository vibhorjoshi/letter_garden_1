# Define utility functions as needed (e.g., data loading, error handling)

def load_config(config_file_path="config.py"):
  """Loads configuration data from a file."""
  # ... (implementation to read configuration from a file)
  return config_data  # Return the loaded configuration data

# ... (add other utility functions as needed)